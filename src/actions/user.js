/*
    TODO: Study 2.

    1. npm install redux, react-redux, redux-thunk, axios, sha256
    2. create action and action creator for register, call register api endpoint with post request,
        send neccessary parameters in the body (email, username, hashedPassword),
        when promise resolves - if the response was successful - dispatch registerSuccess action,
        if not - dispatch registerError action.
    3. store action's type as a constant in the constants folder
*/



/*
    TODO: Task 1.

    1. create action creator for login, using the corresponding login api endpoint.
    2. create actions for success and error case, dispatch these actions.
    3. if the response was successful, set token in the localStorage
*/
