import React from "react";

/* TODO: Task 2.
    1. create Header component,
    2. create Header.css file and import it to Header.js
    3. Header component should have a button for uploading a new image.
    4. export Header component as default (and import it in App.js.)
*/

const Header = () => (
    <div>
        {/* */}
    </div>
)
