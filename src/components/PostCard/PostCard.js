import React from "react";

/* TODO: Study 1b.
  1. create reusable PostCard component
  2. PostCard component receives {image, title, description } through props,
  3. export it as default ( and import in Posts.js )
  4. Style it with plain CSS, bootstrap or anything else, your choice
*/

const PostCard = ({ image, title }) => (
  <div>
    {/* */}
  </div>
);

